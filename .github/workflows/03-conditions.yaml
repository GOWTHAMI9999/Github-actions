name: Conditions
on: [push]
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: step 1
        if: github.ref == 'refs/head/feature-**'
        run: echo "hello"
      - name: step 2
        if: github.ref == 'refs/head/main'
        run: echo "Bye"