name: Conditions
on: [push]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: step 1
        if: startsWith(github.ref, 'refs/heads/feature-')
        run: echo "hello"

      - name: step 2
        if: github.ref == 'refs/heads/main'
        run: echo "Bye"